{
  "name": "portfolio-monorepo",
  "version": "1.0.0",
  "description": "Root package.json specifically for Azure deployment",
  "scripts": {
    "start": "cd backend && node server.js",
    "postinstall": "cd backend && npm install",
    "test": "echo \"no test specified\" && exit 0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {}
}